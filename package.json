{
  "name": "portfolio-blog",
  "version": "1.0.0",
  "description": "Personal portfolio with blog",
  "scripts": {
    "build": "node scripts/build-blog.js && node scripts/build-projects.js && node scripts/build-sitemap.js",
    "dev": "npm run build && npx serve ."
  },
  "dependencies": {},
  "devDependencies": {
    "serve": "^14.0.0"
  }
}
